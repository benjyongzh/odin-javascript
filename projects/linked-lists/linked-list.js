function linkedList(){
    // append(value)
    // prepend(value)
    // size()
    // head()
    // tail()
    // at(index)
    // pop()
    // contains(value)
    // find(value)
    // toString()
    // insertAt(value,index)
    // removeAt(index)
};

function node(value){
    //value
    //next
};